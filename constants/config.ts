// ── API Keys & Configuration ─────────────────────────────────
// Set these via environment variables (EXPO_PUBLIC_ prefix for Expo)
// or replace the placeholders for development.

/** Vapi API key — get yours at https://dashboard.vapi.ai */
export const VAPI_API_KEY = process.env.EXPO_PUBLIC_VAPI_API_KEY || '';

export const VAPI_BASE_URL = 'https://api.vapi.ai';
